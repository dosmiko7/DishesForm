import { Button } from "@mui/material";

const FormSubmitButton = () => {
	return (
		<Button
			variant="contained"
			type="submit"
		>
			Submit dish
		</Button>
	);
};

export default FormSubmitButton;
